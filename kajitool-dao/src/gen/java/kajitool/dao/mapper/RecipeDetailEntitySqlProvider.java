package kajitool.dao.mapper;

import java.util.List;
import java.util.Map;
import kajitool.dao.model.RecipeDetailEntity;
import kajitool.dao.model.RecipeDetailEntityExample.Criteria;
import kajitool.dao.model.RecipeDetailEntityExample.Criterion;
import kajitool.dao.model.RecipeDetailEntityExample;
import org.apache.ibatis.jdbc.SQL;

public class RecipeDetailEntitySqlProvider {

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String countByExample(RecipeDetailEntityExample example) {
        SQL sql = new SQL();
        sql.SELECT("count(*)").FROM("PUBLIC.RECIPE_DETAIL");
        applyWhere(sql, example, false);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String deleteByExample(RecipeDetailEntityExample example) {
        SQL sql = new SQL();
        sql.DELETE_FROM("PUBLIC.RECIPE_DETAIL");
        applyWhere(sql, example, false);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String insertSelective(RecipeDetailEntity record) {
        SQL sql = new SQL();
        sql.INSERT_INTO("PUBLIC.RECIPE_DETAIL");
        
        if (record.getId() != null) {
            sql.VALUES("ID", "#{id,jdbcType=BIGINT}");
        }
        
        if (record.getRecipeId() != null) {
            sql.VALUES("RECIPE_ID", "#{recipeId,jdbcType=BIGINT}");
        }
        
        if (record.getMaterialId() != null) {
            sql.VALUES("MATERIAL_ID", "#{materialId,jdbcType=BIGINT}");
        }
        
        if (record.getQuantity() != null) {
            sql.VALUES("QUANTITY", "#{quantity,jdbcType=INTEGER}");
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String selectByExample(RecipeDetailEntityExample example) {
        SQL sql = new SQL();
        if (example != null && example.isDistinct()) {
            sql.SELECT_DISTINCT("ID");
        } else {
            sql.SELECT("ID");
        }
        sql.SELECT("RECIPE_ID");
        sql.SELECT("MATERIAL_ID");
        sql.SELECT("QUANTITY");
        sql.FROM("PUBLIC.RECIPE_DETAIL");
        applyWhere(sql, example, false);
        
        if (example != null && example.getOrderByClause() != null) {
            sql.ORDER_BY(example.getOrderByClause());
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String updateByExampleSelective(Map<String, Object> parameter) {
        RecipeDetailEntity record = (RecipeDetailEntity) parameter.get("record");
        RecipeDetailEntityExample example = (RecipeDetailEntityExample) parameter.get("example");
        
        SQL sql = new SQL();
        sql.UPDATE("PUBLIC.RECIPE_DETAIL");
        
        if (record.getId() != null) {
            sql.SET("ID = #{record.id,jdbcType=BIGINT}");
        }
        
        if (record.getRecipeId() != null) {
            sql.SET("RECIPE_ID = #{record.recipeId,jdbcType=BIGINT}");
        }
        
        if (record.getMaterialId() != null) {
            sql.SET("MATERIAL_ID = #{record.materialId,jdbcType=BIGINT}");
        }
        
        if (record.getQuantity() != null) {
            sql.SET("QUANTITY = #{record.quantity,jdbcType=INTEGER}");
        }
        
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String updateByExample(Map<String, Object> parameter) {
        SQL sql = new SQL();
        sql.UPDATE("PUBLIC.RECIPE_DETAIL");
        
        sql.SET("ID = #{record.id,jdbcType=BIGINT}");
        sql.SET("RECIPE_ID = #{record.recipeId,jdbcType=BIGINT}");
        sql.SET("MATERIAL_ID = #{record.materialId,jdbcType=BIGINT}");
        sql.SET("QUANTITY = #{record.quantity,jdbcType=INTEGER}");
        
        RecipeDetailEntityExample example = (RecipeDetailEntityExample) parameter.get("example");
        applyWhere(sql, example, true);
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    public String updateByPrimaryKeySelective(RecipeDetailEntity record) {
        SQL sql = new SQL();
        sql.UPDATE("PUBLIC.RECIPE_DETAIL");
        
        if (record.getRecipeId() != null) {
            sql.SET("RECIPE_ID = #{recipeId,jdbcType=BIGINT}");
        }
        
        if (record.getMaterialId() != null) {
            sql.SET("MATERIAL_ID = #{materialId,jdbcType=BIGINT}");
        }
        
        if (record.getQuantity() != null) {
            sql.SET("QUANTITY = #{quantity,jdbcType=INTEGER}");
        }
        
        sql.WHERE("ID = #{id,jdbcType=BIGINT}");
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table PUBLIC.RECIPE_DETAIL
     *
     * @mbg.generated
     */
    protected void applyWhere(SQL sql, RecipeDetailEntityExample example, boolean includeExamplePhrase) {
        if (example == null) {
            return;
        }
        
        String parmPhrase1;
        String parmPhrase1_th;
        String parmPhrase2;
        String parmPhrase2_th;
        String parmPhrase3;
        String parmPhrase3_th;
        if (includeExamplePhrase) {
            parmPhrase1 = "%s #{example.oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{example.oredCriteria[%d].allCriteria[%d].value} and #{example.oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{example.oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{example.oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{example.oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{example.oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        } else {
            parmPhrase1 = "%s #{oredCriteria[%d].allCriteria[%d].value}";
            parmPhrase1_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s}";
            parmPhrase2 = "%s #{oredCriteria[%d].allCriteria[%d].value} and #{oredCriteria[%d].criteria[%d].secondValue}";
            parmPhrase2_th = "%s #{oredCriteria[%d].allCriteria[%d].value,typeHandler=%s} and #{oredCriteria[%d].criteria[%d].secondValue,typeHandler=%s}";
            parmPhrase3 = "#{oredCriteria[%d].allCriteria[%d].value[%d]}";
            parmPhrase3_th = "#{oredCriteria[%d].allCriteria[%d].value[%d],typeHandler=%s}";
        }
        
        StringBuilder sb = new StringBuilder();
        List<Criteria> oredCriteria = example.getOredCriteria();
        boolean firstCriteria = true;
        for (int i = 0; i < oredCriteria.size(); i++) {
            Criteria criteria = oredCriteria.get(i);
            if (criteria.isValid()) {
                if (firstCriteria) {
                    firstCriteria = false;
                } else {
                    sb.append(" or ");
                }
                
                sb.append('(');
                List<Criterion> criterions = criteria.getAllCriteria();
                boolean firstCriterion = true;
                for (int j = 0; j < criterions.size(); j++) {
                    Criterion criterion = criterions.get(j);
                    if (firstCriterion) {
                        firstCriterion = false;
                    } else {
                        sb.append(" and ");
                    }
                    
                    if (criterion.isNoValue()) {
                        sb.append(criterion.getCondition());
                    } else if (criterion.isSingleValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase1, criterion.getCondition(), i, j));
                        } else {
                            sb.append(String.format(parmPhrase1_th, criterion.getCondition(), i, j,criterion.getTypeHandler()));
                        }
                    } else if (criterion.isBetweenValue()) {
                        if (criterion.getTypeHandler() == null) {
                            sb.append(String.format(parmPhrase2, criterion.getCondition(), i, j, i, j));
                        } else {
                            sb.append(String.format(parmPhrase2_th, criterion.getCondition(), i, j, criterion.getTypeHandler(), i, j, criterion.getTypeHandler()));
                        }
                    } else if (criterion.isListValue()) {
                        sb.append(criterion.getCondition());
                        sb.append(" (");
                        List<?> listItems = (List<?>) criterion.getValue();
                        boolean comma = false;
                        for (int k = 0; k < listItems.size(); k++) {
                            if (comma) {
                                sb.append(", ");
                            } else {
                                comma = true;
                            }
                            if (criterion.getTypeHandler() == null) {
                                sb.append(String.format(parmPhrase3, i, j, k));
                            } else {
                                sb.append(String.format(parmPhrase3_th, i, j, k, criterion.getTypeHandler()));
                            }
                        }
                        sb.append(')');
                    }
                }
                sb.append(')');
            }
        }
        
        if (sb.length() > 0) {
            sql.WHERE(sb.toString());
        }
    }
}